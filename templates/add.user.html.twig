{% extends "adminbase.html.twig" %}

{% block title %}
    Felhasználó létrehozása - Webmenza
{% endblock title %}

{% block headtitle %}
    Felhasználó létrehozása
{% endblock headtitle %}

{% block content %}
<form method="post" action="#">
    <div class="mb-3 row">
        <label for="name" class="form-label col-sm-2">Név:</label>
        <input type="text" name="name" id="name" class="form-control col" required value="{{ post.name }}">
    </div>
    <div class="mb-3 row">
        <label for="password" class="form-label col-sm-2">Jelszó:</label>
        <input type="password" name="password" id="password" class="form-control col" required autocomplete="new-password">
    </div>
    <div class="mb-3 row">
        <label for="password2" class="form-label col-sm-2">Jelszó újra:</label>
        <input type="password" name="password2" id="password2" class="form-control col" required autocomplete="new-password">
    </div>
    <div class="mb-3 row">
        <label for="group" class="form-label col-sm-2">Csoport:</label>
        <select name="group" id="group" class="form-select col">
            <option value=""></option>
            {% for group in groups %}
                <option value="{{ group['id'] }}" {% if post.group == group['id'] %} selected {% endif %}>{{ group['name'] }}</option>
            {% endfor %}
        </select>
    </div>
    <div class="form-check mb-3">
        <input class="form-check-input" type="checkbox" value="1" id="registered" name="registered">
        <label class="form-check-label" for="registered">
          Aktivált felhasználó
        </label>
    </div>
    <button type="submit" class="btn btn-primary"><i class="bi bi-floppy"></i> Mentés</button>
</form>
{% endblock content %}